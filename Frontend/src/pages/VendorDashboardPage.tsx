/**
 * VendorDashboardPage
 *
 * Landing page for /vendor. Will show the vendor's own product/order
 * summary once features/vendors is built out.
 */
export function VendorDashboardPage() {
  return (
    <div>
      <h2 className="text-lg font-semibold text-slate-900">Vendor Overview</h2>
      <p className="mt-2 text-sm text-slate-500">Vendor summary widgets go here.</p>
    </div>
  );
}
